# Chapter Server Configuration
# Copy this file to .env for local development

# =============================================================================
# DATABASE (SQLite)
# =============================================================================

# SQLite database file path
# For local dev, this creates a file in the current directory
DATABASE_URL="file:./data/chapter.db"

# =============================================================================
# CACHE
# =============================================================================

# Redis connection string
REDIS_URL="redis://localhost:6379"

# =============================================================================
# SECURITY
# =============================================================================

# JWT secret for signing authentication tokens
# Generate with: openssl rand -base64 48
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# JWT token expiration (e.g., "7d", "24h", "1h")
JWT_EXPIRES_IN="7d"

# =============================================================================
# STORAGE
# =============================================================================

# Path to store uploaded EPUB files
BOOK_STORAGE_PATH="./data/books"

# Path to cache generated audio files
AUDIO_CACHE_PATH="./data/audio"

# Maximum audio cache size in bytes (default: 10 GB)
# Cache uses LRU eviction when limit is reached
AUDIO_CACHE_MAX_SIZE=10737418240

# =============================================================================
# TEXT-TO-SPEECH (Kokoro)
# =============================================================================

# URL of the Kokoro TTS service
# Local dev: http://localhost:5000
# Docker:    http://kokoro:5000
KOKORO_SERVICE_URL="http://localhost:5000"

# Default voice for new users
# Options: af_bella, af_nicole, af_sarah, af_sky, am_adam, am_michael, bf_emma, bf_isabella, bm
TTS_DEFAULT_VOICE="af_bella"

# =============================================================================
# SERVER
# =============================================================================

# Port to run the API server
PORT=3001

# Environment: development | production
NODE_ENV="development"
